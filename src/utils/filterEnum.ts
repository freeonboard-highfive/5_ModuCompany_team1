export enum Status {
  FINISHED = 'Done',
  ONGOING = 'Doing',
  NOT_STARTED = 'Todo',
  ALL = 'All',
}

export enum DateType {
  GoalDate = 'goalDate',
  CreatedAt = 'createdAt',
  UpdatedAt = 'updatedAt',
}

export enum IMPORTANCE {
  All = 'All',
  true = 'Important',
  false = 'NotImportant',
}
