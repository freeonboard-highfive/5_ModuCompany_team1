export enum Status {
  FINISHED = 'Done',
  ONGOING = 'Doing',
  NOT_STARTED = 'Todo'
}

export enum DateType {
  GoalDate = 'goalDate',
  CreatedAt ='createdAt',
  UpdatedAt = 'updatedAt'
}

export enum IMPORTANCE {
  true = 1,
  false = 0,
}
