export enum Status {
  Finished = 'Done',
  Ongoing = 'Doing',
  NotStarted = 'Todo'
}

export enum DateType {
  GoalDate = 'goalDate',
  CreatedAt ='createdAt',
  UpdatedAt = 'updatedAt'
}

export enum Importance {
  False,
  True
}
